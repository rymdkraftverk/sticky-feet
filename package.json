{
  "scripts": {
    "qa": "npm test && npm run lint && npm run typecheck",
    "test": "jest",
    "lint": "npm run lint --prefix game && npm run lint --prefix controller",
    "typecheck": "npm run typecheck --prefix game"
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "babel-jest": "^24.8.0",
    "jest": "^24.8.0",
    "ramda": "^0.26.1"
  }
}
